import CustomSection from "components/CustomSection";
function Hero() {
  return (
    <CustomSection sectionName="hero">
      <>this the Hero page</>
    </CustomSection>
  );
}

export default Hero;
